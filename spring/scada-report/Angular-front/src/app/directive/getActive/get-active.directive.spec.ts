import { GetActiveDirective } from './get-active.directive';

describe('GetActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GetActiveDirective();
    expect(directive).toBeTruthy();
  });
});
